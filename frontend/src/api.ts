// Compatibility re-export: all consumers that import from 'src/api.ts' continue to work.
// Actual implementation lives in src/api/ — see src/api/index.ts.
// V2: Single entry point maintained.
export * from './api/index'

